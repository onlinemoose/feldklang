---
import '@fontsource-variable/inter';

// 'DM Sans'
// Nunito
// Dosis
// Outfit
// Roboto
// Literata
// 'IBM Plex Sans'
// Karla
// Poppins
// 'Fira Sans'
// 'Libre Franklin'
// Inconsolata
// Raleway
// Oswald
// 'Space Grotesk'
// Urbanist
---

<style is:inline>
  :root {
    --aw-font-sans: 'Inter Variable';
    --aw-font-serif: 'Inter Variable';
    --aw-font-heading: 'Inter Variable';

    --aw-color-primary: rgb(1 97 239);
    --aw-color-secondary: rgb(1 84 207);
    --aw-color-accent: rgb(109 40 217);

    --aw-color-text-heading: rgb(0 0 0);
    --aw-color-text-default: rgb(16 16 16);
    --aw-color-text-muted: rgb(16 16 16 / 66%);
    --aw-color-bg-page: #f1e7cb;
    --aw-color-bg-page-secondary: #f0f3e5;
    --aw-color-bg-page-dark: rgb(3 6 32);

    ::selection {
      background-color: lavender;
    }
  }

  .dark {
    --aw-font-sans: 'Inter Variable';
    --aw-font-serif: 'Inter Variable';
    --aw-font-heading: 'Inter Variable';

    --aw-color-primary: rgb(1 97 239);
    --aw-color-secondary: rgb(1 84 207);
    --aw-color-accent: rgb(109 40 217);

    --aw-color-text-heading: #f5efe2;
    --aw-color-text-default: #e8e0cd;
    --aw-color-text-muted: rgb(232 224 205 / 66%);
    --aw-color-bg-page: #1b1712;
    --aw-color-bg-page-dark: #1b1712;

    ::selection {
      background-color: #000000;
      color: snow;
    }
  }

  /* Image overlay effects */
  .image-blur-diagonal {
    position: relative;
    overflow: hidden;
    border-radius: 0.5rem;
  }

  /* Opacity fade on the image itself */
  .image-blur-diagonal img {
    mask-image:
      linear-gradient(
        135deg,
        transparent 0%,
        rgba(0, 0, 0, 0.15) 15%,
        rgba(0, 0, 0, 0.6) 30%,
        rgba(0, 0, 0, 1) 45%,
        rgba(0, 0, 0, 1) 55%,
        rgba(0, 0, 0, 0.6) 70%,
        rgba(0, 0, 0, 0.15) 85%,
        transparent 100%
      ),
      linear-gradient(
        315deg,
        transparent 0%,
        rgba(0, 0, 0, 0.15) 15%,
        rgba(0, 0, 0, 0.6) 30%,
        rgba(0, 0, 0, 1) 45%,
        rgba(0, 0, 0, 1) 55%,
        rgba(0, 0, 0, 0.6) 70%,
        rgba(0, 0, 0, 0.15) 85%,
        transparent 100%
      );
    -webkit-mask-image:
      linear-gradient(
        135deg,
        transparent 0%,
        rgba(0, 0, 0, 0.15) 15%,
        rgba(0, 0, 0, 0.6) 30%,
        rgba(0, 0, 0, 1) 45%,
        rgba(0, 0, 0, 1) 55%,
        rgba(0, 0, 0, 0.6) 70%,
        rgba(0, 0, 0, 0.15) 85%,
        transparent 100%
      ),
      linear-gradient(
        315deg,
        transparent 0%,
        rgba(0, 0, 0, 0.15) 15%,
        rgba(0, 0, 0, 0.6) 30%,
        rgba(0, 0, 0, 1) 45%,
        rgba(0, 0, 0, 1) 55%,
        rgba(0, 0, 0, 0.6) 70%,
        rgba(0, 0, 0, 0.15) 85%,
        transparent 100%
      );
    mask-composite: intersect;
    -webkit-mask-composite: source-in;
  }

  /* Top-left blur */
  .image-blur-diagonal::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.2) 0%, transparent 50%);
    backdrop-filter: blur(22px);
    -webkit-backdrop-filter: blur(22px);
    mask-image: linear-gradient(135deg, rgba(0, 0, 0, 1) 0%, rgba(0, 0, 0, 0.7) 30%, transparent 60%);
    -webkit-mask-image: linear-gradient(135deg, rgba(0, 0, 0, 1) 0%, rgba(0, 0, 0, 0.7) 30%, transparent 60%);
    z-index: 1;
    pointer-events: none;
    border-radius: 0.5rem;
  }

  .dark .image-blur-diagonal::before {
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.2) 0%, transparent 50%);
  }

  /* Bottom-right blur */
  .image-blur-diagonal::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(315deg, rgba(255, 255, 255, 0.2) 0%, transparent 50%);
    backdrop-filter: blur(22px);
    -webkit-backdrop-filter: blur(22px);
    mask-image: linear-gradient(315deg, rgba(0, 0, 0, 1) 0%, rgba(0, 0, 0, 0.7) 30%, transparent 60%);
    -webkit-mask-image: linear-gradient(315deg, rgba(0, 0, 0, 1) 0%, rgba(0, 0, 0, 0.7) 30%, transparent 60%);
    z-index: 1;
    pointer-events: none;
    border-radius: 0.5rem;
  }

  .dark .image-blur-diagonal::after {
    background: linear-gradient(315deg, rgba(0, 0, 0, 0.2) 0%, transparent 50%);
  }

  /* Diagonal line overlay - top-right to bottom-left */
  .image-blur-diagonal-line {
    position: relative;
    overflow: hidden;
    border-radius: 0.5rem;
  }

  /* Inherit the blur diagonal effects + add transparent diagonal cut from top-left to bottom-right */
  .image-blur-diagonal-line img {
    mask-image:
      linear-gradient(
        45deg,
        rgba(0, 0, 0, 1) 0%,
        rgba(0, 0, 0, 1) calc(50% - 1px),
        transparent calc(50% - 1px),
        transparent calc(50% + 1px),
        rgba(0, 0, 0, 1) calc(50% + 1px),
        rgba(0, 0, 0, 1) 100%
      ),
      linear-gradient(
        135deg,
        transparent 0%,
        rgba(0, 0, 0, 0.15) 15%,
        rgba(0, 0, 0, 0.6) 30%,
        rgba(0, 0, 0, 1) 45%,
        rgba(0, 0, 0, 1) 55%,
        rgba(0, 0, 0, 0.6) 70%,
        rgba(0, 0, 0, 0.15) 85%,
        transparent 100%
      ),
      linear-gradient(
        315deg,
        transparent 0%,
        rgba(0, 0, 0, 0.15) 15%,
        rgba(0, 0, 0, 0.6) 30%,
        rgba(0, 0, 0, 1) 45%,
        rgba(0, 0, 0, 1) 55%,
        rgba(0, 0, 0, 0.6) 70%,
        rgba(0, 0, 0, 0.15) 85%,
        transparent 100%
      );
    -webkit-mask-image:
      linear-gradient(
        45deg,
        rgba(0, 0, 0, 1) 0%,
        rgba(0, 0, 0, 1) calc(50% - 1px),
        transparent calc(50% - 1px),
        transparent calc(50% + 1px),
        rgba(0, 0, 0, 1) calc(50% + 1px),
        rgba(0, 0, 0, 1) 100%
      ),
      linear-gradient(
        135deg,
        transparent 0%,
        rgba(0, 0, 0, 0.15) 15%,
        rgba(0, 0, 0, 0.6) 30%,
        rgba(0, 0, 0, 1) 45%,
        rgba(0, 0, 0, 1) 55%,
        rgba(0, 0, 0, 0.6) 70%,
        rgba(0, 0, 0, 0.15) 85%,
        transparent 100%
      ),
      linear-gradient(
        315deg,
        transparent 0%,
        rgba(0, 0, 0, 0.15) 15%,
        rgba(0, 0, 0, 0.6) 30%,
        rgba(0, 0, 0, 1) 45%,
        rgba(0, 0, 0, 1) 55%,
        rgba(0, 0, 0, 0.6) 70%,
        rgba(0, 0, 0, 0.15) 85%,
        transparent 100%
      );
    mask-composite: intersect;
    -webkit-mask-composite: source-in;
  }

  /* Top-left blur */
  .image-blur-diagonal-line::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.2) 0%, transparent 50%);
    backdrop-filter: blur(22px);
    -webkit-backdrop-filter: blur(22px);
    mask-image: linear-gradient(135deg, rgba(0, 0, 0, 1) 0%, rgba(0, 0, 0, 0.7) 30%, transparent 60%);
    -webkit-mask-image: linear-gradient(135deg, rgba(0, 0, 0, 1) 0%, rgba(0, 0, 0, 0.7) 30%, transparent 60%);
    z-index: 1;
    pointer-events: none;
    border-radius: 0.5rem;
  }

  .dark .image-blur-diagonal-line::before {
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.2) 0%, transparent 50%);
  }

  /* Bottom-right blur */
  .image-blur-diagonal-line::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(315deg, rgba(255, 255, 255, 0.2) 0%, transparent 50%);
    backdrop-filter: blur(22px);
    -webkit-backdrop-filter: blur(22px);
    mask-image: linear-gradient(315deg, rgba(0, 0, 0, 1) 0%, rgba(0, 0, 0, 0.7) 30%, transparent 60%);
    -webkit-mask-image: linear-gradient(315deg, rgba(0, 0, 0, 1) 0%, rgba(0, 0, 0, 0.7) 30%, transparent 60%);
    z-index: 1;
    pointer-events: none;
    border-radius: 0.5rem;
  }

  .dark .image-blur-diagonal-line::after {
    background: linear-gradient(315deg, rgba(0, 0, 0, 0.2) 0%, transparent 50%);
  }
</style>
